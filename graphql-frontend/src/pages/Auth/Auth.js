import React from "react";
import { Typography } from "@material-ui/core";
const Auth = () => {
  return (
    <Typography color="primary" variant="h2">
      auth
    </Typography>
  );
};

export default Auth;
